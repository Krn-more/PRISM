version: '3.8'

services:
  neo4j:
    image: neo4j:latest
    container_name: xchem_neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/xchem1234
    volumes:
      - ./neo4j_data:/data
    restart: unless-stopped
